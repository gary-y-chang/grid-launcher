---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: cartridges.catrdg.cnri.platform
spec:
  group: catrdg.cnri.platform
  names:
    kind: Cartridge
    listKind: CartridgeList
    plural: cartridges
    singular: cartridge
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Cartridge is the Schema for the cartridges API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CartridgeSpec defines the desired state of Cartridge
            properties:
              description:
                description: To describe the nature of the Simulation, showing in
                  UI.
                type: string
              display_name:
                description: The name to show in the Range configuration interface
                type: string
              hosts:
                description: The virtual machines to be created.
                items:
                  properties:
                    baseImage:
                      type: string
                    condition:
                      type: string
                    disk:
                      type: string
                    instanceType:
                      description: The type of the host, e.g., "u1.xlarge"
                      type: string
                    labels:
                      items:
                        type: string
                      type: array
                    name:
                      description: The name of the host.
                      type: string
                    networks:
                      items:
                        properties:
                          address:
                            description: The IP address specified or dhcp assigned.
                            type: string
                          interface:
                            description: The type of the network, e.g., "public" or
                              "private".
                            type: string
                          name:
                            description: The name of the network.
                            type: string
                        required:
                        - address
                        - interface
                        - name
                        type: object
                      type: array
                    vdiAccess:
                      type: string
                  required:
                  - baseImage
                  - disk
                  - instanceType
                  - labels
                  - name
                  - networks
                  type: object
                type: array
              logo:
                description: The base-64 image of a Simulation being easily identifiable.
                type: string
              networks:
                description: The networking configuration for the simulation environment.
                items:
                  description: |-
                    EDIT THIS FILE!  THIS IS SCAFFOLDING FOR YOU TO OWN!
                    NOTE: json tags are required.  Any new fields you add must have json tags for the fields to be serialized.
                  properties:
                    cidrBlock:
                      type: string
                    gateway:
                      type: string
                    name:
                      type: string
                    protocol:
                      type: string
                  required:
                  - cidrBlock
                  - gateway
                  - name
                  - protocol
                  type: object
                type: array
              playbooks:
                description: The Ansible playbooks for the virtual machines configuration
                type: string
              values:
                description: Values by default and available in SimRequest to substitute
                  variables defined in the containers manifest of a Cartridge.
                type: string
            required:
            - hosts
            - networks
            - values
            type: object
          status:
            description: CartridgeStatus defines the observed state of Cartridge
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
