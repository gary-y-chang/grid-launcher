---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: simrequests.simreq.cnri.platform
spec:
  group: simreq.cnri.platform
  names:
    kind: SimRequest
    listKind: SimRequestList
    plural: simrequests
    singular: simrequest
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: SimRequest is the Schema for the simrequests API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SimRequestSpec defines the desired state of SimRequest
            properties:
              cartridge_name:
                description: The name of the requested cartridge to activate
                type: string
              grid:
                description: The namespace where the cartridge resources will be deployed
                  and the simulation will run.
                type: string
              simulation:
                description: The idetifier or description of a Simulation to be launched.
                type: string
              values:
                description: Values in yaml are used to substitute variables defined
                  in the containers manifest of a Cartridge.
                type: string
            required:
            - cartridge_name
            - grid
            type: object
          status:
            description: SimRequestStatus defines the observed state of SimRequest
            properties:
              applied_values:
                type: string
              result:
                type: string
              start_time:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                type: string
            required:
            - result
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
